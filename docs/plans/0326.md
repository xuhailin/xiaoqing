# 小晴 Schema 收口与依赖重组执行计划 Prompt（Claude Code 专用）

请基于当前仓库代码现状，以及“主链路已接近稳定架构、可以进入 Schema 收口阶段”的前提，**直接产出一份新的执行计划文档**。

请先快速阅读与 assistant 主链路、orchestrator、assembler、response composer、post-turn pipeline、chat completion engine 相关的核心文件，再产出计划；不要只根据单文件猜测。

## 你的唯一任务

在仓库中新增一份文档：

`docs/plan/schema-and-dependency-convergence-plan.md`

如果已存在同类文档，则新建一份不冲突的新文件名，并在文档开头说明与已有文档的关系。

---

## 输出目标

这份文档不是审计报告，也不是概念讨论，而是：

> **供 Codex / Claude Code 后续逐 Phase 执行的工程计划**

要求这份计划必须满足：

1. **可执行**：任务粒度适合 Codex 逐项实现
2. **可验收**：每个任务都有明确验收标准
3. **可分批提交**：每个 Phase 都可以独立提交 PR
4. **可兼容过渡态**：允许 adapter / mapper / 旧层兼容，不要求一步推倒
5. **以当前代码现状为基础**：不要脱离现有实现重画理想架构

---

## 当前前提（必须遵守）

请以以下事实为前提，不要重复审计这些方向是否正确：

### 已基本成立的方向

* Quick Intent Router 已前置接入 `assistant-orchestrator.processTurn()` 最开头
* 高置信规则命中可绕过完整意图 LLM
* `ActionReasoner.decide()` 已移出 `TurnContextAssembler`
* `ResponseComposer` 中的 CognitivePipeline fallback 分析已消除
* `captureStructuredWorkItem` 已纳入 `PostTurnPipeline.beforeReturn`
* 当前状态可判断为：**已接近稳定架构**
* 可以开始：**结构化 Schema 收口 + 依赖方向收敛**

### 当前剩余遗留

* `ChatCompletionEngine` 在 chat 路径仍有部分执行 / 表达 / 编排混合职责
* 该问题当前风险低、影响面小
* 本轮计划中应将其视为：**冻结增责的过渡层**
* 不要求为它单独开专项重构 sprint
* 但后续所有新设计不得继续加重它的职责

---

## 本轮计划的核心目标

请围绕下面两个目标产出计划：

### 目标 1：结构化 Schema 收口

把主链路中已经基本稳定的中间对象，正式收敛为清晰的 TypeScript type / interface / contract。

### 目标 2：依赖方向收敛

让主链路逐步从“隐式上下文 + 散乱参数 + 历史耦合”，收敛为“围绕结构化对象流动”。

---

## 你需要覆盖的核心对象

计划中必须覆盖以下对象，但请结合代码现状决定优先级与落地方式：

* `QuickRouterOutput`
* `PerceptionState`
* `DecisionState`
* `ExecutionResult`
* `ExpressionParams`
* `PostTurnUpdatePlan`

---

## 你需要覆盖的核心问题

计划中必须回答并落实这些问题：

1. 这些对象分别放在哪些文件 / 模块更合适？
2. 谁生产？谁消费？
3. 哪些模块之间应该改为传结构化对象，而不是继续传散乱参数？
4. 哪些旧模块需要 adapter / mapper 过渡？
5. 哪些遗留结构暂时保留，不要急着拆？
6. 哪些任务适合 Codex 独立逐项执行？

---

## 输出文档要求

请直接生成 markdown 文档内容，写入 `docs/plan/schema-and-dependency-convergence-plan.md`。

文档必须包含以下章节，且顺序固定。

---

# 1. 文档目标

用简短文字说明：

* 这份计划解决什么问题
* 为什么现在进入 Schema 收口阶段
* 为什么本轮不做“大爆破式重构”

要求简洁，控制在 10 行左右。

---

# 2. 当前阶段判断

这一节必须明确写出：

* 当前为什么可以开始 Schema 收口
* 当前哪些问题已经不再是结构阻塞项
* 当前哪个遗留层被视为“过渡保留层”

这一节不要写成长篇审计，只写结论。

---

# 3. 目标对象清单

以表格输出以下内容：

| 对象 | 所属层 | 作用 | 当前现状 | 建议落点 | 优先级 |
| -- | --- | -- | ---- | ---- | --- |

必须覆盖：

* QuickRouterOutput
* PerceptionState
* DecisionState
* ExecutionResult
* ExpressionParams
* PostTurnUpdatePlan

要求：

* 建议落点尽量具体到模块或文件目录
* 优先级要真实，不要全部写高

---

# 4. 目标依赖方向

请用简洁的树状结构或流程结构描述目标依赖方向，例如：

```text
QuickIntentRouter
  -> TurnPerception
  -> TurnDecision
  -> CapabilityExecutor
  -> ExpressionControl
  -> PostTurnPipeline
```

然后补充说明：

* 每层允许依赖什么
* 每层不应依赖什么
* 哪些旧模块先通过 adapter 过渡

这一节不要空谈，要尽量贴近当前仓库已有模块名称。

---

# 5. 分阶段执行计划（最重要）

这是整份文档最重要的部分。
请按 **Phase 1 / Phase 2 / Phase 3** 输出。

## 计划设计要求

### 总要求

* Phase 数量固定为 3
* 每个 Phase 不超过 5 个任务
* 每个任务都必须适合 Codex 独立执行
* 每个任务都必须限制边界，避免“一次改太多”

### 每个任务必须包含以下字段

* 任务名
* 修改范围（文件 / 模块）
* 本次动作
* 明确不做什么
* 验收标准
* 风险说明
* 是否建议单独 PR（是 / 否）

### Phase 设计目标

* **Phase 1：Schema 收口**

  * 只做核心对象 type/interface 收敛
  * 不做大规模调用迁移
* **Phase 2：依赖迁移**

  * 让核心链路改为消费结构化对象
  * 可以增加 adapter / mapper
* **Phase 3：过渡层收敛**

  * 清理重复 DTO、散乱参数、临时桥接
  * 冻结遗留层增责，但不要求彻底删除旧层

---

# 6. Codex 执行建议

这一节必须专门为 Codex 写，告诉后续执行者如何分批做。

请输出：

## 推荐执行顺序

* 先做哪些任务
* 哪些任务必须串行
* 哪些任务可以并行

## 单任务粒度建议

* 什么样的任务适合一次交给 Codex
* 什么样的任务不要合并在一个 prompt 里

## 风险控制建议

* 哪些模块改动前要先读调用链
* 哪些任务必须先补日志 / 类型 / 测试，再动实现
* 哪些地方最容易“表面结构化，实际没减耦”

这一节要尽量实战，不要泛泛而谈。

---

# 7. 本轮不做什么

这一节非常重要，必须明确列出：

* 本轮不做的大改动
* 本轮不强拆的旧模块
* 本轮不追求的理想化目标
* 哪些问题虽然存在，但应延后处理

要求避免后续 Codex 越做越大。

---

# 8. 验收总标准

请从整体角度列出本轮计划完成后的验收标准，例如：

* 至少 5 个核心对象已稳定落类型
* 主链路关键节点开始围绕结构化对象流动
* 新增逻辑不再继续向 ChatCompletionEngine 增责
* Expression 层输入更统一
* Post-turn 写回入口更集中

要求可验证、可观察，不要写空话。

---

# 9. 简短总结

最后请补一个很短的总结，包含：

* 现在最值得先做的 3 个任务
* 现在不要急着做的 2 个任务
* 推荐先交给 Codex 的第一个 Phase

---

## 关键执行原则（必须遵守）

1. **不要输出审计报告口吻**

   * 重点是“怎么收口”
   * 不是“哪里有问题”

2. **不要写成理想架构论文**

   * 必须贴近当前仓库已有模块
   * 允许过渡层存在

3. **任务必须适合 Codex**

   * 任务要小
   * 边界要清楚
   * 验收要具体
   * 不要一个任务横跨太多层

4. **先 Schema，后迁移**

   * 不要在 Phase 1 就安排大规模依赖重组

5. **冻结遗留层增责**

   * 尤其是 `ChatCompletionEngine`
   * 本轮不要继续把新职责塞进去

6. **优先保障主链路稳定**

   * 不要为了代码好看做破坏性重构

---

## 最终输出要求

你的最终产出应该是：

* 直接创建/更新 `docs/plan/schema-and-dependency-convergence-plan.md`
* 文档内容完整
* 可直接作为后续 Codex 执行计划基础
* 不需要修改业务代码
* 不需要生成 patch
* 不需要额外解释

---

