import { ConfigService } from '@nestjs/config';
import type { OpenClawTaskRequest, OpenClawTaskResult, OpenClawToolInvokeRequest } from './openclaw.types';
export declare class OpenClawService {
    private readonly logger;
    private readonly botId;
    private readonly token;
    private readonly signKey;
    private readonly pluginBaseUrl;
    private readonly taskPath;
    private readonly useChatStyle;
    private readonly effectivePath;
    private readonly defaultTimeout;
    private readonly isWecomPath;
    private readonly wecomToken;
    private readonly wecomEncodingAesKey;
    private readonly wecomCorpId;
    private readonly wecomAgentId;
    private readonly wecomBodyStyle;
    private readonly enabled;
    constructor(config: ConfigService);
    private wecomMsgSignature;
    private escapeXmlCdata;
    private wecomEncrypt;
    delegateTask(req: OpenClawTaskRequest): Promise<OpenClawTaskResult>;
    private doRequest;
    private parseChatCompletionsResponse;
    invokeTool(req: OpenClawToolInvokeRequest): Promise<OpenClawTaskResult>;
    isAvailable(): boolean;
    checkHealth(): Promise<boolean>;
}
