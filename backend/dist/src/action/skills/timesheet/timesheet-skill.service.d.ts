import { ConfigService } from '@nestjs/config';
import { PrismaService } from '../../../infra/prisma.service';
import type { ICapability } from '../../capability.interface';
import type { CapabilityRequest, CapabilityResult } from '../../capability.types';
import type { MessageChannel } from '../../../gateway/message-router.types';
import { type TimesheetProjectMapping } from './timesheet.config';
import type { TimesheetSkillExecuteParams, TimesheetSkillResult, TimesheetOverrideEntry } from './timesheet-skill.types';
export declare class TimesheetSkillService implements ICapability {
    private readonly config;
    private readonly prisma;
    private readonly logger;
    private readonly featureEnabled;
    private readonly loginId;
    readonly name = "timesheet";
    readonly taskIntent = "timesheet";
    readonly channels: MessageChannel[];
    readonly description = "\u5DE5\u65F6\u4E0A\u62A5\uFF08\u586B\u5DE5\u65F6\u3001\u5F55\u5165\u5DE5\u65F6\u3001\u67E5\u8BE2\u672A\u5F55\u5165\u5DE5\u65F6\u7B49\uFF09";
    constructor(config: ConfigService, prisma: PrismaService);
    isAvailable(): boolean;
    execute(request: CapabilityRequest): Promise<CapabilityResult>;
    executeTimesheet(params: TimesheetSkillExecuteParams): Promise<TimesheetSkillResult>;
    private previewTimesheet;
    private confirmTimesheet;
    private submitWithOverrides;
    parseRawOverride(raw: string, mappings: TimesheetProjectMapping[]): TimesheetOverrideEntry[];
    private parseSingleOverrideLine;
    fuzzyMatchProject(hint: string, mappings: TimesheetProjectMapping[]): TimesheetProjectMapping | null;
    private overlapScore;
    private submitTimesheet;
    private queryMissingDates;
    private loadMappings;
    private getWorkdaysInMonth;
    private todayString;
    private currentMonthString;
    private formatDate;
    private buildExternalSubmitError;
    private parseParams;
}
