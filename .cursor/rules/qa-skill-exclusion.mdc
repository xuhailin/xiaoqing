---
description: QA 回归测试 — 有副作用的技能禁止纳入自动化测试
alwaysApply: true
---

# QA 技能排除约定

以下技能具有真实外部副作用（浏览器操作、文件下载、第三方系统交互），**永远不要**将它们添加到 QA 回归测试流程中：

- `book-download`（电子书下载）
- `checkin`（考勤打卡）
- `timesheet`（工时上报）

具体要求：

1. **禁止创建 QA case**：不要在 `qa/cases/curated/`、`qa/cases/promoted/` 或 `qa/replays/` 中为上述技能创建测试场景。
2. **禁止在 gate/replay 中触发**：已有的 QA case 不应包含会触发上述技能的用户输入（如"帮我打卡"、"下载xxx书"、"提交工时"）。
3. **新增有副作用的技能时**：如果后续新增的技能涉及真实外部操作（登录、支付、文件写入、第三方 API 调用等），应同步更新本规则文件，将其加入排除列表。

原因：这些技能一旦在测试中被触发，会产生不可逆的真实操作（打卡记录、下载流量、工时提交），且依赖外部登录态和环境配置，不适合自动化回归。
