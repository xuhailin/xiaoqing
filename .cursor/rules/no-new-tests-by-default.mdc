---
description: 项目约定 — 无明确要求时不新增测试文件
alwaysApply: true
---

# 测试文件约定

- **默认不新增测试文件**：后续设计或实现新模块时，如无明确要求（需求文档、PR 说明、用户或评审方明确要求等），**不要**主动新增或补写 `*.spec.ts`、`*.test.ts` 等测试文件。
- **已有测试**：现有模块已有的测试可保留并维护；修改既有逻辑时若已有对应单测，可一并更新。
- **有明确要求时**：当需求/PR/用户明确要求「补充单测」「加测试」「覆盖某模块」时，再为该模块新增或补充测试文件。
